\colorlet{shadecolor}{\chapterColor}
\onecolumn
\chapter{\VAR{area.name}}\label{a:\VAR{area.name}}
\markboth{\color{white}\VAR{area.name} \protect\thepage \hspace{4pt}}{}
\lhead{\textcolor{\chapterColor}{\rule[-2pt]{\textwidth}{15pt}}}
%% if area.bannerPhoto
\begin{figure}[h]
\includegraphics[width=\textwidth]{./Image/\VAR{area.bannerPhoto.fileName}}
\end{figure}
%% endif
\twocolumn
\VAR{area.description or "\\"}

%% for subArea in area.subareas.values()
%% if subArea.name
\section{\VAR{subArea.name}}\label{sa:\VAR{subArea.name}}
%% endif

\VAR{subArea.description or "\\"}


%% for photo in subArea.photos
\begin{figure}[t]
\includegraphics[width=\linewidth]{./Image/\VAR{photo.fileName}}
\captionsetup{labelformat=empty}
\caption{\textbf{\VAR{photo.description}}}
\end{figure}
%% endfor

%% if subArea.boulders
%% for boulder in subArea.boulders.values()
\subsection*{\VAR{boulder.name}}\label{bf:\VAR{boulder.name}}
\VAR{boulder.description or "\\"}


%% for photo in boulder.photos
\begin{figure}[h]
\includegraphics[width=\linewidth]{./Image/\VAR{photo.fileName}}
\captionsetup{labelformat=empty}
\caption{\textbf{\VAR{photo.description}}}
\end{figure}
%% endfor

%% if boulder.routes
%% if loop.index ==1 
\begin{enumerate}[]
%% else
\begin{enumerate}[resume]
%% endif

%% for route in boulder.routes.values()
	\item\label{rt:\VAR{route.name}} \colorbox{\VAR{route.color}}{\textbf{\VAR{route.name} V\VAR{route.grade} \BLOCK{for x in range(route.rating)} \ding{72} \BLOCK{ endfor }} }
	\newline \VAR{route.description}\
%% if route.variations.values()
	\newline \textbf{Variations:}
	\begin{enumerate}
%% for variation in route.variations.values()
		\item\label{vr:\VAR{variation.name}} \colorbox{\VAR{variation.color}}{\emph{\VAR{variation.name} V\VAR{variation.grade}}  }
		\newline \VAR{variation.description}\
%% endfor
	\end{enumerate}
%% endif
%% endfor 
\end{enumerate}
%% endif

%% endfor 
%% endif
%% endfor


\clearpage