%% import 'macros.tex' as m
%#
\VAR{m.resolve_images_area_head(area, size='s')}
\VAR{m.resolve_images_area_head(area, size='p')}
\VAR{m.resolve_images_area_head(area, size='pr')}
\VAR{m.chapter_head( area.name)}
\VAR{m.resolve_images_area_head(area, size='f')}
\raggedcolumns
\begin{multicols}{2}
\VAR{m.gps(area)}
%% if area.routes.values() and ('suppress_histogram' not in area.format_options)
\includegraphics[width=\linewidth]{\VAR{area.paths['histogram_o']}/\VAR{area.item_id}.png}
\end{multicols}
\begin{multicols}{2}
%% endif
\VAR{m.resolve_images_area_head(area, size='h')}
\VAR{m.body(area)}
\VAR{m.resolve_images(area, 'a')}

%% for subArea in area.subareas.values()
%% if 'suppress_page_break' not in subArea.format_options
\newpage
%% else
\needspace{6em}
%% if 'section_break' in subArea.format_options
\end{multicols}
\hrule 
\needspace{6em}
\begin{multicols}{2}
%% endif
%% endif
\VAR{m.resolve_images(subArea)}
%% if subArea.name
\section{\VAR{subArea.getSubAreaLtr()} - \VAR{subArea.name}}\phantomsection\label{sa:\VAR{subArea.item_id}}
%% endif
\VAR{m.gps(subArea)}
\VAR{m.body(subArea)}
\VAR{m.resolve_images(subArea, 'a')}

%% for formation in subArea.formations.values()
\VAR{m.resolve_images(formation)}
\needspace{10em}
%% if formation.name
\subsection*{\VAR{formation.name}}\phantomsection\label{bf:\VAR{formation.item_id}}
%% endif
\VAR{m.gps(formation)}
\VAR{m.body(formation)}
\VAR{m.resolve_images(formation, 'a')}
%% for route in formation.routes.values()
\VAR{m.resolve_images(route)}
\VAR{m.climbLabel(route)}
\VAR{m.resolve_images(route, 'a')}
%% if route.variations.values()
\begin{adjustwidth}{0.5cm}{}				
\needspace{4em}
\textbf{Variations:} \newline
%% for variation in route.variations.values()
\VAR{m.resolve_images(variation)}
\VAR{m.climbLabel(variation)}
\VAR{m.resolve_images(variation, 'a')}
\VAR{m.resolve_images(variation, 'e')}
%% endfor
\end{adjustwidth}
%% endif
\VAR{m.resolve_images(route, 'e')}
%% endfor 
\VAR{m.resolve_images(formation, 'e')}
%% endfor 
\VAR{m.resolve_images(subArea, 'e')}
%% endfor
\VAR{m.resolve_images(area, 'e')}
\end{multicols}
\clearpage